{
  "name": "rca-email-app",
  "version": "1.0.0",
  "description": "RCA Platform with Email Functionality",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npm run build:frontend",
    "build:frontend": "cp -r frontend dist && cp dist/index.html dist.html",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "@sendgrid/mail": "^7.7.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}